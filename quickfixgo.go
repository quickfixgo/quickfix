package quickfixgo

import(
    "quickfixgo/message"
    "quickfixgo/session"
    )

func SendToTarget(msgBuilder message.Builder, sessionID session.ID) (bool, SessionNotFound) {
  return false, nil
}
